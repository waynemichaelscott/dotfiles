# Learn how to configure Hyprland: https://wiki.hyprland.org/Configuring/

# Use defaults Omarchy defaults (but don't edit these directly!)
source = ~/.local/share/omarchy/default/hypr/autostart.conf
source = ~/.local/share/omarchy/default/hypr/bindings/media.conf
source = ~/.local/share/omarchy/default/hypr/bindings/tiling.conf
source = ~/.local/share/omarchy/default/hypr/bindings/utilities.conf
source = ~/.local/share/omarchy/default/hypr/bindings/clipboard.conf
source = ~/.local/share/omarchy/default/hypr/envs.conf
source = ~/.local/share/omarchy/default/hypr/looknfeel.conf
source = ~/.local/share/omarchy/default/hypr/input.conf
source = ~/.local/share/omarchy/default/hypr/windows.conf
source = ~/.config/omarchy/current/theme/hyprland.conf

# Change your own setup in these files (and overwrite any settings from defaults!)
source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/input.conf
source = ~/.config/hypr/bindings.conf
source = ~/.config/hypr/envs.conf
source = ~/.config/hypr/autostart.conf
source = ~/.config/hypr/looknfeel.conf

#env = ANDROID_EMULATOR_USE_SYSTEM_LIBS,1
env = QT_QPA_PLATFORM,wayland;xcb
env = _JAVA_AWT_WM_NONREPARENTING,1

# Scratchpad — distinct look: amber border + floating panel with larger gaps
windowrule = border_color rgba(ffaa33ee) rgba(ff880066), match:workspace special:scratchpad
workspace = special:scratchpad, gapsIn:8, gapsOut:80

# Terminal windows — auto-group together
windowrule = group set, match:class ^(kitty)$

# SSH terminals — red border to make them obvious
windowrule = border_color rgba(ff5555ee) rgba(ff555566), match:title ^(SSH:.*)$

# Android Emulator → workspace 3, floating
windowrule = workspace 3, match:class ^(Emulator)$
windowrule = float on, match:class ^(Emulator)$

# Lazygit + Lazydocker → workspace 5, grouped together
windowrule = workspace 5, match:class ^(org\.omarchy\.lazygit)$
windowrule = group set, match:class ^(org\.omarchy\.lazygit)$
windowrule = workspace 5, match:class ^(org\.omarchy\.lazydocker)$
windowrule = group set, match:class ^(org\.omarchy\.lazydocker)$

windowruleV2 = float, class:^(xfreerdp)$
windowruleV2 = size 3840 2160, class:^(xfreerdp)$
windowruleV2 = center, class:^(xfreerdp)$
windowruleV2 = nomaximizerequest, class:^(xfreerdp)$
windowruleV2 = noshadow, class:^(xfreerdp)$

# Rule: Send xfreerdp to workspace 9 and make it silent (don't jump there immediately)
# This catches the window the very instant it is mapped to the screen
# Force the window to move to workspace 9 the moment it is created
windowruleV2 = workspace 8, class:^(xfreerdp)$, title:^(.*FF754.*)$
windowruleV2 = monitor 0, class:^(xfreerdp)$
windowruleV2 = fullscreen, class:^(xfreerdp)$